version: '3.8'

services:
  jarvis:
    build: .
    ports:
      - "8765:8765"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - JARVIS_MCP_TRANSPORT=http
    restart: unless-stopped
